import React from 'react';

const ExitButton: React.FC = () => {
  return (
    <button className="bg-black text-white p-2 rounded-full fixed top-4 left-4 shadow-md">
      Exit
    </button>
  );
};

export default ExitButton;
